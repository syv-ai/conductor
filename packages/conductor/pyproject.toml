[project]
name = "syv-conductor"
version = "0.1.4"
description = "Reusable DAG execution engine — node registration, graph compilation, eager parallel streaming execution, retry, shared references, and human-in-the-loop checkpointing."
requires-python = ">=3.12"
authors = [{ name = "Syvai", email = "billy@syv.ai" }]
license = "Apache-2.0"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = ["pydantic>=2.0"]

[project.optional-dependencies]
yaml = ["pyyaml>=6.0"]
# Sibling-package extras — published in lockstep from this monorepo, so
# pinned `==` to the matching version to prevent resolver skew.
nodes = ["syv-conductor-nodes==0.1.4"]
providers = ["syv-conductor-providers==0.1.4"]
all = [
    "syv-conductor-nodes==0.1.4",
    "syv-conductor-providers[fastapi]==0.1.4",
]

[project.urls]
Repository = "https://github.com/syvai/conductor"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/conductor"]
